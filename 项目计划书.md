# 个性化算法学习助手 设计文档

---

## 一、针对什么问题

本人在数据结构与算法的学习中存在以下四个核心困难：

1. **偏图像化思维，但现有学习资料以文字为主**：教材、博客、OJ 题解大多是代码块 + 文字描述，看得很吃力。像 KMP 的 next 数组构建、Dijkstra 的松弛过程，光看文字很难在脑子里形成动态画面。
2. **理解驱动型人格，但现有反馈太粗暴**：提交 OJ 只返回 AC/WA/TLE，不告诉我"错在哪里"和"为什么错了"。死记模板能过题，但下次变形题还是不会。
3. **容易犯重复性小错误**：边界值漏判、循环条件写反、int 溢出、忘记重置 visited 数组等等。每次都是同一个坑反复踩。
4. **不会 Debug**：拿到 WA 后不知道如何定位问题，只能一行行加 printf，效率极低，挫败感强。

总结来说，我需要一个**能画出算法执行过程、能用提问引导我思考、能记住我经常犯错的地方**的学习助手。

---

## 二、解决什么痛点

| 现状痛点 | 本项目解决方案 |
|----------|---------------|
| 刷题不会做就看题解，看完以为自己懂了，下次还是写不出来 | 用苏格拉底式追问引导自主思考，而非直接给出答案 |
| 教材/OJ上的知识是纯文字，想象不出执行过程 | 用结构化图示（ASCII 流程图 + 状态表 + 递归树）呈现算法运行轨迹 |
| WA 之后不知道错在哪，Debug 方法只会 printf | AI 分析代码逻辑，定位疑似出错行号，解释原因并教 Debug 技巧 |
| 同一类错误反复犯，没有记录累积 | 本地错误记忆库追踪高频错误模式，主动预警和生成针对性练习 |

---

## 三、如何解决痛点：核心功能 & 设计思路

### 3.1 总体设计思路：适配"视觉化 + 理解驱动"的认知风格

学习认知风格分析的结论：

- **视觉化**意味着系统应尽可能用图、表、结构化的方式呈现信息，而非大段文字。
- **理解驱动**意味着系统不能直接给答案，而应该通过层层递进的提问，帮我自主构建知识体系。

因此系统设计遵循以下原则：
- **禁止直接输出完整代码**，改为输出执行流程图 + 逐行逻辑分析 + 引导性问题。
- **所有知识点讲解必须附带可视化表示**（状态转移表、递归调用栈图示、动画化文字描述）。
- **错误分析不走"给你正确答案"路线，而是走"先让你看哪里不对 → 再让你自己想 → 最后再给你检查"路线**。

### 3.2 核心功能模块

```
┌─────────────────────────────────────────────┐
│              CLI 交互界面 (Rich)              │
│    Markdown渲染 + 代码高亮 + 流程图展示        │
├─────────────────────────────────────────────┤
│             业务调度层 (Controller)           │
│         路由用户意图，串联各模块               │
├──────────────────┬──────────────────────────┤
│  Prompt 引擎      │   本地错误记忆库          │
│  · 知识点可视化    │   · SQLite 存储           │
│  · 苏格拉底式提问  │   · 错误模式聚类         │
│  · 代码错误分析    │   · 重复错误预警         │
│  · 针对性练习生成  │   · 练习推荐引擎         │
└──────────────────┴──────────────────────────┘
```


### 功能一：可视化知识点讲解（适配图像化思维）

**功能描述**：当用户请求讲解某个算法或数据结构时，AI 不输出大段文字，而是生成结构化的可视化说明。

**输出示例**（以 Dijkstra 算法为例）：

```
初始状态:
  dist = [0, ∞, ∞, ∞, ∞]
  visited = [F, F, F, F, F]

Step 1: 选取 dist 最小且未访问的节点 0 (dist=0)
         ↓ 更新邻居
  dist[1] = min(∞, 0+2) = 2  ✓
  dist[2] = min(∞, 0+4) = 4  ✓
  visited = [T, F, F, F, F]

Step 2: 选取 dist 最小且未访问的节点 1 (dist=2)
         ↓ 更新邻居
  dist[2] = min(4, 2+1) = 3  ✓ 更新！
  dist[3] = min(∞, 2+7) = 9  ✓
  visited = [T, T, F, F, F]
  ...

引导性问题：为什么 dist[2] 在 Step 2 中从 4 更新为 3？
```

**设计思路**：用分步的状态表 + 箭头表示数据流向来替代纯文本描述，每一步都附带一个小问题确保用户在跟着思考。


### 功能二：苏格拉底式提问引擎（适配理解驱动）

**功能描述**：无论用户是请求讲解、请求 Debug 还是请求解题帮助，系统强制走"提问引导"流程，不直接给出答案。

**三阶段引导模型**：

1. **定位阶段**："你的代码在第 17 行的循环条件中写了 `i <= n`，但数组下标是 0 到 n-1。你觉得当 i = n 时会发生什么？"
2. **推理阶段**："如果边界问题解决了仍然 WA，在输入 `n=1, m=0` 这种极端情况下，你的 visited 数组会被初始化吗？检查第 8 行。"
3. **归纳阶段**："总结一下，你这次遇到的两次 WA 分别是边界检查不完整和初始化遗漏。下次遇到图遍历问题，你可以先问自己哪两个问题来主动避免这两类错误？"


### 功能三：代码错误分析与 Debug 引导（解决"不会 Debug"）

**功能描述**：用户提交代码 + 题目描述，系统分析代码逻辑并输出引导式排错建议。

**流程**：

1. 用户输入：`analyze my_solution.cpp`
2. 系统将代码 + 题目描述发给 LLM，要求按以下结构返回：
   - 逻辑走向分析（用流程图文字描述你的代码实际上在做什么）
   - 与题目要求的差异点
   - 疑似出错行号及原因
   - 1-2 个引导性问题（而非直接给正确代码）
   - 可选的边界测试用例建议

**交互示例**：

```
$ ds-tutor analyze my_dp.cpp

┌ 逻辑走向分析 ───────────────────────────────┐
│ 你的 dp[i] 定义为"前 i 个物品的最大价值"      │
│ 但在转移方程 dp[i] = max(dp[i-1], ...) 中,    │
│ 你没有考虑"不选第 i 个物品"的情况             │
│                                               │
│ 疑似问题：第 22 行，转移方程缺少 dp[i-1] 项   │
├ 引导问题 ────────────────────────────────────┤
│ Q: 如果第 i 个物品重量 > 背包剩余容量，        │
│    dp[i] 应该等于什么？                       │
│    A. dp[i-1]                                │
│    B. 0                                      │
│    C. dp[i] 保持不变                          │
└──────────────────────────────────────────────┘
```

### 功能四：错误模式记忆与预警（解决"反复犯同类错误"）

**功能描述**：系统在每次 Debug 会话后记录用户的错误类型，建立个人错误档案。当用户下次提交同类型问题时主动预警。

**数据结构**：

```
错误记录表 (SQLite):
  - 时间
  - 题目类型 (dp / 图论 / 贪心 / 数据结构)
  - 错误类型 (边界条件 / 初始化 / 溢出 / 逻辑错误 / 特判遗漏)
  - 具体描述
  - 是否已掌握
```

**预警示例**：

```
$ ds-tutor analyze my_graph.cpp

⚠️  记忆提醒: 你之前在3道图论题中都犯了"visited数组未重置"的错误
    (2024-05-10 岛屿数量 / 2024-05-15 迷宫问题 / 2024-05-20 拓扑排序)
    
    → 请先检查第12行的 visited 初始化，确认无误后再继续分析。
```

**设计思路**：这是全系统唯一"个性化"的核心——它积累的不是通用的知识库，而是**你个人**的错误模式，实现真正意义上的"适配自身认知特点"。

---

### 3.3 技术选型

| 层次 | 技术 | 理由 |
|------|------|------|
| 界面 | Python Rich 库 | 终端内 Markdown 渲染 + 代码高亮 + Panel/Layout |
| LLM 调用 | OpenAI 兼容 API | 使用 Prompt Engineering 实现各模块 |
| 数据存储 | SQLite | 轻量、零配置，存错误记忆和练习记录 |
| 语言 | Python | 快速原型开发，Rich 和 SQLite 生态成熟 |

说明：相比初版方案的 Docker 沙箱 + 自动化对拍，本方案大幅简化技术栈。原因：
- 这是一个 **学习辅助工具**，不是 OJ 评测系统，不需要在线运行代码。
- 代码分析通过 LLM 的代码理解能力完成，不需要实际编译执行。
- CLI + SQLite 足够支撑所有功能，降低开发和部署成本。

---

## 四、实现效果与截图（预期）

| 场景 | 预期效果 |
|------|---------|
| 请求讲解 KMP 算法 | 终端展示 next 数组构建过程的逐行动画 + 引导问题 |
| 提交 WA 代码分析 | 输出逻辑走向图 + 疑似行号 + 引导提问 + 错误记忆预警 |
| 请求专项练习 | 根据错误记忆库生成变体题目，附带上次同类错误的提醒 |
| 阶段性学习总结 | 展示错误分布饼图 (ASCII Chart) + 掌握情况变化趋势 |

*（截图将在实现后补充）*

---

## 五、自我评价总结与展望

### 5.1 与自身学习特点的匹配度自评

| 学习特点 | 系统适配方式 | 预期效果 |
|---------|-------------|---------|
| 偏图像化 | 所有输出附带流程图/状态表/递归树等结构化图示 | 减少"看文字看不懂"的摩擦 |
| 理解驱动 | 苏格拉底式三阶段提问，杜绝直接给答案 | 倒逼深度思考，建立可迁移的知识 |
| 容易犯小错误 | 错误记忆库追踪高频错误，主动预警 | 同类错误不再犯第三次 |
| 不会 Debug | 引导式错误分析，教授 Debug 思维而非给答案 | 逐步建立自主 Debug 能力 |

### 5.2 项目局限与展望

**当前局限**：
- 依赖 LLM API，无法离线使用，且回复质量受模型能力影响。
- 代码分析完全依靠 LLM 静态阅读，不实际编译运行，可能遗漏运行时错误。
- 错误模式识别比较粗糙，仅按类型 + 标签分类，未到"解题思路级"的深度分析。

**未来展望**：
- 增加代码实际编译运行能力（沙箱），结合运行时报错做更精准的 Debug 分析。
- 接入 TTS，让系统能以"语音提问"的方式做真正的苏格拉底式对话练习。
- 错误模式使用 embedding 做语义聚类，发现更隐蔽的知识盲区。
- 支持多门课程扩展，不局限于数据结构与算法。

---

*（初版由 AI 生成框架，经本人结合自身学习特点重新设计）*
